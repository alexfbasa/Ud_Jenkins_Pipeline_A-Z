stage ('Check for existence of /etc/environment') {
    agent any
    steps {
        script {
            if (fileExists('/etc/environment') {
                echo "File /etc/environment found!"
            }
        }
    }
}