def file = $FILE_DIR
stage ('Check for existence of index.html') {
    agent any
    steps {
        script {
            if (fileExists("$file") {
                echo "File $file found!"
            }
        }
    }
}
