pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                tools {
                    maven 'Maven 3'
                }
            }
            steps {
                sh 'mvn --version'
            }
        }
    }
}
