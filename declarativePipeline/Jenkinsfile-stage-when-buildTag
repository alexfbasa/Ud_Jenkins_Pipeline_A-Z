pipeline {
    agent any
    stages {
        stage('Build') {
            when {
                // Habilitar Discover Tag
                buildingTag()
            }
            steps {
                echo "Building Deploying tag"
            }
        }
    }
}
