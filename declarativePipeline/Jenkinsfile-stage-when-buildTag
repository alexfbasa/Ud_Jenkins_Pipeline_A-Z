pipeline {
    agent any
    stages {
        stage('Build') {
            when {
                // Habilitar Discover Tag
                buildingTag(1.0)
            }
            steps {
                echo "Building Deploying tag"
            }
        }
    }
}
