pipeline {
    agent any
    stages {
        stage('Build') {
            when {
                // Habilitar Discover Tag
                tag "2.0"
            }
            steps {
                echo "Building Deploying tag"
            }
        }
    }
}
