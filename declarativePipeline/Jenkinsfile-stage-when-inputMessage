pipeline {
    agent any

    stages {
        stage('Build') {
            input {
                message "Please specify environment:"
                ok "OK"
                submitter "dummyuser,admin@localhost.com"
                submitterParameter "whoIsSubmitter"
                parameters {
                    string{name: 'environment'}
                }

            }

            steps {
                echo "We are building for , and we are deploying to environment: "
                echo "region:, myText: , , and myFile:"
                echo "submitter is: "
                echo "selected credentials is: "
            }
        }
    }
}